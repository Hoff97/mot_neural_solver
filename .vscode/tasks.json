{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run training",
            "type": "shell",
            "command": "${config:python.pythonPath}",
            "args": [
                "scripts/train.py"
            ],
            "problemMatcher": []
        },
        {
            "label": "Remove experiments",
            "type": "shell",
            "command": "${config:python.pythonPath}",
            "args": [
                "-c",
                "from mot_neural_solver.path_cfg import OUTPUT_PATH; import shutil; shutil.rmtree(OUTPUT_PATH + \"/experiments\")"
            ],
            "problemMatcher": []
        },
        {
            "label": "Run tensorboard",
            "type": "shell",
            "command": "${config:python.pythonPath}",
            "args": [
                "-c",
                "from mot_neural_solver.path_cfg import OUTPUT_PATH; from tensorboard import program; tb = program.TensorBoard(); tb.configure(argv=[None, \"--logdir\", OUTPUT_PATH]); url = tb.launch(); from threading import Event; Event().wait()"
            ],
            "problemMatcher": []
        }
    ]
}